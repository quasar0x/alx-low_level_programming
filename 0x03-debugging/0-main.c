#include "main.h"

/**
 * This is the beginning of the main program.
 * It includes main.h, which should contain the function prototype for positive_or_negative.
 */

/**
 * main - Test function for positive or negative
 * Return: 0
 */
int main(void)
{
    int i;

    /* Initialize i to 0 */
    i = 0;

    /* Call positive_or_negative function with i */
    positive_or_negative(i);

    /* End of main function */
    return (0);
}
